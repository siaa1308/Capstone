# Historical utilities

These XGBoost and experiment-summary scripts are retained for provenance but are not part of the final Temporal GNN → CL → FedAvg pipeline. Their original default artifact paths may refer to locations now organized under `artifacts/historical/`.

